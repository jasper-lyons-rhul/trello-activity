<svg height="100" width="700">
  <polyline
    fill="none"
    stroke="#0074d9"
    stroke-width="3"
    points="
    <% ((Date.today-7)..(Date.today)).each_with_index do |date, index| %>
      <%= index * 100 %>, <%= actions_at(date).size %>
    <% end %>
    "
  />
</svg>
