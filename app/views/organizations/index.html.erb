<script>
  window.addEventListener('load', function () {
    actions.setKeyAndToken({
      oauth_token: '<%= @oauth_token %>',
      oauth_token_secret: '<%= @oauth_token_secret %>'
    });
  });
</script>
